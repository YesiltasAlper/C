#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main() {

	/*
	int n;
	printf("Dizi boyut giriniz : ");
	scanf("%d", &n);
	int* ptr = (int*)malloc(sizeof(int) * n);		

	if (ptr == NULL) {
		printf("Basarisiz...");
		exit(EXIT_FAILURE);
	}

	for (int i = 0; i < n; i++)
		ptr[i] = i + 14;

	for (int i = 0; i < n; i++)
		printf("ptr[%d] = %d\n", i, ptr[i]);

	free(ptr);										
	*/



	/*
	struct point {
		int mx;
		int my;
	};

	struct point *ps = (struct point *)malloc(sizeof(struct point));		// Heap'den 8 byte'lik bellek tahsilati istendi.

	if (ps == NULL) {
		printf("Basarisiz...");
		return 0;
	}

	ps->mx = 30;
	ps->my = 40;

	printf("%d\t%d", ps->mx,ps->my);
	
	free(ps);
	*/
}

